-children property of the props object

-conditional rendering

-module css (ComponentName.module.css)(import style from '...')(className={styles.component-name})

-MUI library "sx prop" to edit a component's css.

-MUI modal(dialog)

-
const formData = new FormData(e.target)
const formObj = Object.fromEntries(formData.entries())